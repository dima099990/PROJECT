# IDE
.idea/
.vscode/

# venv / Python
venv/
.venv/
__pycache__/
*.pyc
*.pyo

# Секреты
.env

# Модели и данные (тяжёлое, локальное)
models/*
*.gguf
# НО: свои модели (scratch) и реестр — пушим в git, чтобы не пересоздавать
!models/custom/
!models/custom/**
data/chroma/
data/trash/
data/chats.json
data/active_model.txt
data/stats.json
data/settings.json
data/training_history.json
data/parse_history.json
data/session_history.md
data/corpus/

# Логи
logs/*.log
logs/*.jsonl

# Память claude-mem пишет folder-CLAUDE.md — НЕ игнорируем CLAUDE.md (память в репо)
# (явных правил для CLAUDE.md нет — они трекаются)

# Оставляем структуру пустых папок
!**/.gitkeep
